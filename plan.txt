/assignment/:assignment
INPUT: params/options from PL
- build (future)
    - create new Dockerfile from template
    - build image from assignment
        - check cache?
- spin up docker container
RETURN: docker ID, or some string that can be mapped to one

/docker/:id
- testing only
- render page with xterm.js

/docker/:id/ws
- attach to docker container with ID
- hook up WS to in/out/err
- on close:
    - stop container

- on close (future):
    - stop container
    - commit container with *name*
    - build autograder 
    - run autograder and read JSON output
    - remove all containers
    - store JSON for later
